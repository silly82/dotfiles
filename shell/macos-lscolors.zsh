# macOS BSD ls: Standard-LSCOLORS nutzt u. a. "d" (braun) als Vorder- oder
# Hintergrund (z. B. Zeilen device/special) — wirkt auf dunklen/Neon-Terminals oft schmutzig.
# Keine braunen Flächen; Pipes statt matten Braun (d) in neutral/grau.
export CLICOLOR=1
export LSCOLORS=exfxcxhxbxegexabagacax
